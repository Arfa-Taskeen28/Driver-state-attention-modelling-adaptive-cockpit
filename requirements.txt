# PyTorch (CPU) must be installed separately first, from its own index:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# (kept out of this file so the CPU wheel is used on every platform)
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10
matplotlib>=3.7
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.5
pydantic-settings>=2.0
httpx>=0.27
pytest>=8.0
